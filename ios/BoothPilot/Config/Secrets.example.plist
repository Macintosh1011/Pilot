<?xml version="1.0" encoding="UTF-8"?>
<!--
  Copy this file to `Secrets.plist` (same folder) and fill in the Vapi public key to enable the
  live voice loop. Secrets.plist is gitignored. With no key the booth runs scripted Phase 1
  (UI + live Convex backend). Non-secret backend URLs live in Info.plist / BoothConfig, not here.
-->
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>VAPI_PUBLIC_KEY</key>
    <string>vapi_public_key_goes_here</string>
</dict>
</plist>
